const fs = require("fs");
const cache = require("./cache");

/**
 * @param {String} filePath
 * @param {Number} size
 * @param {Number} blockSize
 * @param {Function} callback
 * @return {Null}
 * @internal
*/
function alloc(filePath,size,blockSize,callback){

}

/**
 * @param {String} filePath
 * @param {Array} blocks
 * @param {Function} callback
 * @return {Null}
 * @internal
*/
function free(filePath,blocks,callback){

}


 